import React from 'react';

function DatasetsPage() {
  return (
    <div>
      <h1 className="text-xl font-bold mb-4">Datasets Page (con sidebar)</h1>
      <p>Aquí podrías mostrar la lista completa de datasets, filtrar, etc.</p>
      <p>
        Por ejemplo, <code>/datasets/123</code> para ver detalle de un dataset.
      </p>
    </div>
  );
}

export default DatasetsPage;
